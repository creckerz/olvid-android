pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version '2.1.10'
    }
}
rootProject.name='Olvid for Android'

include ':app', ':engine', ':sardine-android'

project(':engine').projectDir = new File("../obv_engine/engine")